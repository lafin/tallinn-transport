export interface Point {
  vehicleType: number;
  lineNumber: string;
  latitude: number;
  longitude: number;
  vehicleNumber: string;
}

export interface ApiResponse {
  VehicleType: number;
  LineNumber: string;
  Latitude: number;
  Longitude: number;
  VehicleNumber: string;
}
